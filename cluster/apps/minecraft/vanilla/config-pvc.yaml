---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-config
  namespace: minefract
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 5Gi
